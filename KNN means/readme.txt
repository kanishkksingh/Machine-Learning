It creates the Centroids with using K value, and find the nearest data points, after that it finds the avg value. 

If the avg distance between centroids and data points are more, then it finds new centroids points and calculate the distance between data points.


KMeans for classification, clustering for grouping multiple data points.